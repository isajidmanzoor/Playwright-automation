import { Endpoint } from '@mintlify/validation';
import { DataSchemaArray } from '@mintlify/validation';

export type SearchResult = {
  title: string;
  content: string;
  link: string;
};

export type ToolWithEndpointWithUuid = {
  tool: Tool;
  uuid: string;
  fileName: string;
  endpoint: Endpoint<DataSchemaArray>;
};

export type InitializationConfiguration = {
  name: string;
  trieveDatasetId: string;
  trieveApiKey: string;
  openApiUrls?: string[];
  tools?: ToolWithEndpointWithUuid[];
};

export type ToolWithEndpoint = {
  tool: Omit<Tool, 'inputSchema'>;
  endpoint: Endpoint<DataSchemaArray>;
};

export type ServerParams = Record<string, z.ZodSchema>;
