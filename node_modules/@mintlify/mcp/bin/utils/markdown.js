import { colors } from '@mintlify/scraping';
import { remark } from 'remark';
import { getTerminalWidth, padString, wrapText } from '../utils.js';
export function colorize(text, ...colorCodes) {
    const start = colorCodes.map((code) => colors[code]).join('');
    return `${start}${text}${colors.default}`;
}
const processor = remark();
export function renderMarkdown(markdown) {
    var _a;
    const ast = processor.parse(markdown);
    const terminalWidth = getTerminalWidth();
    const boxWidth = terminalWidth - 4;
    const contentWidth = boxWidth - 2;
    const rendered = renderNode(ast, 0);
    const paragraphs = rendered.split('\n\n');
    const topBorder = '┌' + '─'.repeat(boxWidth) + '┐';
    const bottomBorder = '└' + '─'.repeat(boxWidth) + '┘';
    let result = topBorder + '\n';
    for (let i = 0; i < paragraphs.length; i++) {
        const paragraph = (_a = paragraphs[i]) !== null && _a !== void 0 ? _a : '';
        if (paragraph === '')
            continue;
        if (paragraph.includes('•') || /^\d+\./.test(paragraph)) {
            const listLines = paragraph.split('\n');
            for (const line of listLines) {
                const wrappedLines = wrapText(line, contentWidth);
                for (const wrappedLine of wrappedLines) {
                    result += `│ ${padString(wrappedLine !== null && wrappedLine !== void 0 ? wrappedLine : '', contentWidth)} │\n`;
                }
            }
        }
        else {
            const wrappedLines = wrapText(paragraph, contentWidth);
            for (const line of wrappedLines) {
                result += `│ ${padString(line !== null && line !== void 0 ? line : '', contentWidth)} │\n`;
            }
        }
        if (i < paragraphs.length - 1) {
            result += `│ ${' '.repeat(contentWidth)} │\n`;
        }
    }
    result += bottomBorder;
    return result;
}
function renderNode(node, depth = 0) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j;
    switch (node.type) {
        case 'root':
            return ((_a = node.children) === null || _a === void 0 ? void 0 : _a.map((child) => renderNode(child, depth)).join('')) || '';
        case 'heading':
            return (colorize(((_b = node.children) === null || _b === void 0 ? void 0 : _b.map((child) => renderNode(child, depth)).join('')) || '', 'bold', 'green') + '\n\n');
        case 'paragraph':
            return (colorize(((_c = node.children) === null || _c === void 0 ? void 0 : _c.map((child) => renderNode(child, depth)).join('')) || '', 'default') + '\n\n');
        case 'strong':
            return colorize(((_d = node.children) === null || _d === void 0 ? void 0 : _d.map((child) => renderNode(child, depth)).join('')) || '', 'bold');
        case 'emphasis':
            return colorize(((_e = node.children) === null || _e === void 0 ? void 0 : _e.map((child) => renderNode(child, depth)).join('')) || '', 'italic');
        case 'link':
            return ((_f = node.children) === null || _f === void 0 ? void 0 : _f.map((child) => renderNode(child, depth)).join('')) || '';
        case 'text':
            return node.value || '';
        case 'list':
            const items = ((_g = node.children) === null || _g === void 0 ? void 0 : _g.map((item, index) => {
                const bullet = node.ordered ? `${index + 1}.` : '•';
                const indent = ' '.repeat(depth * 2);
                const itemContent = renderNode(item, depth + 1);
                return `${indent}${colorize(bullet, 'default')} ${itemContent}`;
            }).join('')) || '';
            return items;
        case 'listItem':
            return ((_h = node.children) === null || _h === void 0 ? void 0 : _h.map((child) => renderNode(child, depth)).join('')) || '';
        case 'code':
            return colorize(node.value || '', 'default') + '\n\n';
        case 'blockquote':
            const quoteContent = ((_j = node.children) === null || _j === void 0 ? void 0 : _j.map((child) => renderNode(child, depth + 1)).join('')) || '';
            const indentedQuote = quoteContent
                .split('\n')
                .map((line) => `> ${line}`)
                .join('\n');
            return colorize(indentedQuote, 'default') + '\n';
        case 'thematicBreak':
            return colorize('---', 'default') + '\n\n';
        case 'image':
            return colorize(`[Image: ${node.alt || 'image'}]`, 'default');
        case 'inlineCode':
            return colorize(node.value || '', 'default');
        default:
            if (node.children && node.children.length > 0) {
                return node.children.map((child) => renderNode(child, depth)).join('');
            }
            if (node.value) {
                return colorize(node.value, 'default');
            }
            return '';
    }
}
